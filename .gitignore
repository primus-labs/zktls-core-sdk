/node_modules
/dist
.DS_Store
/.vscode
*-key.json
package-lock.json
build

# Environment variables (contains app credentials, do not commit)
.env
.env.local
.env.*.local

# Local-only tooling (soak scripts, personal experiments — not tracked)
